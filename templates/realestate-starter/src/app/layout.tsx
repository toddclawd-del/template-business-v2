import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
});

export const metadata: Metadata = {
  title: "Prestige Realty | Luxury Real Estate in Los Angeles",
  description: "Discover exceptional luxury properties in Los Angeles. Prestige Realty specializes in high-end homes, estates, and penthouses in Beverly Hills, Malibu, Bel Air, and beyond.",
  keywords: "luxury real estate, Los Angeles homes, Beverly Hills, Malibu, Bel Air, penthouses, estates",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} ${playfair.variable} font-sans`}>
        <Navbar />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
